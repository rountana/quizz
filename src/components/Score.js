import React from "react";

export default function Score(props) {
  //   return <div onLoad={props.gameover}> Score: {props.score} </div>;
  return <div> Score: {props.score} </div>;
  //call end of game
  //   <button onClick={()=>     <Quiz quiz={quiz}></Quiz>
  // }> Play Again </button>)
}
